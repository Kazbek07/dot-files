# Start a dbus session
exec $dbus
# Execute swayidle
exec $idle
# Restore default brightness
#exec light -S $brightness
# Start a notification daemon
exec mako
#exec_always --no-startup-id setxkbmap "us,ru" ",winkeys" "grp:alt_shift_toggle,grp_led:scroll
#start yambar
exec yambar ~/.config/yambar/config.yml
exec gammastep
exec pipewire